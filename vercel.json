{
  "version": 2,
  "buildCommand": "pnpm --filter @ap/api build",
  "outputDirectory": "public",
  "installCommand": "pnpm install && pnpm --filter @ap/api exec prisma generate",
  "builds": [
    {
      "src": "backend/src/lambda.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/src/lambda.ts"
    }
  ]
}

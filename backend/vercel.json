{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "50mb", "maxDuration": 60 }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/main.ts" }
  ]
}
